export default function Home() {
  return (
    <main style={{ padding: 24 }}>
      <h1>LINE Fortune Bot PRO + DB + Stripe</h1>
      <p>Webhook: <code>/api/line/webhook</code>, Cron: <code>/api/cron/daily</code>, Stripe: <code>/api/stripe/webhook</code></p>
    </main>
  );
}
